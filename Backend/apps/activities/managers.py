"""
activities — managers. Owner: models: Dev A · endpoints: Dev B.

Custom managers and querysets — only when they are non-trivial.
Soft-delete managers already come from `core.models.BaseModel`.
"""
