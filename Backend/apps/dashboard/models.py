"""
dashboard — models. Owner: Dev B.

**No models.** This app is views only — it aggregates across `trips`,
`budget` and `geo` for the home screen. Kept as its own app so the
dashboard endpoint never collides with the trips app.
"""
