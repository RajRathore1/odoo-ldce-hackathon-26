"""
geo — permissions. Owner: models: Dev A · endpoints: Dev B.

App-specific permissions. Prefer `core/permissions.py` — ownership means
the same thing in every app, so it should have one implementation.
"""
